{
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install-all": "npm run install:root && npm run install:backend && npm run install:frontend",
    "install:root": "npm install",
    "install:backend": "cd backend && npm install",
    "install:frontend": "cd frontend && npm install",
    "build": "cd backend && npm run build && cd .. && node -e \"require('fs').cpSync('backend/dist', 'dist', {recursive: true})\"",
    "build:all": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "start:dev": "npm run start:backend &",
    "start:backend": "cd backend && npm run start:dev",
    "start:frontend": "cd frontend && npm run dev"
  }
}
