# Hooks Directory

## Purpose
Global reusable custom React hooks (e.g. useLocalStorage).

## Responsibilities
This directory is responsible for managing elements related to hooks.

## Rules
- Must start with "use" naming prefix.
- Must be stateless or manage state locally.
